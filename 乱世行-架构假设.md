# 《乱世行：一郡风云》架构假设 v0.3.5

> **本架构唯一尚未证明的事：玩家在第一座城外走十分钟，好不好玩。**
> 这句话永久置顶。它防的是：把一个架构精美但不好玩的游戏做得越来越完整。

**状态**：长期方向：ACCEPTED HYPOTHESES ｜ 当前施工 authority：M0 only

---

## 0. 当前施工权（Authority Split）

**CURRENT CONTROLLING SCOPE = M0**

**M0 有施工权：**

- 玩家移动与镜头
- 当前小地图
- 可见世界遭遇
- 最小实时战斗
- 最小事件回响
- M0 实际需要的 WorldState
- M0 实际需要的本地存档

**FUTURE HYPOTHESES（当前无施工权）：**

- Hot / Warm / Cold 模拟精度分层
- StrategicArmySystem / FactionDecisionSystem / EconomySystem / HistoryEventSystem
- AutoResolver
- 区域升降级
- persistent cross-region army
- 完整 Fact Journal
- Region Pack loader 抽象
- 全天下历史轨道
- data/global 与 regions/ 的“定义 / 出现”拆分（见 §3.3 注）

**未来结构仅因写在本文中，不自动获得实现正当性。只有当前可玩内容产生真实需求后，才允许晋升为 controlling architecture。**

明确禁止：“我顺便先创建接口，方便以后扩展。”——出现即打回。

---

## 1. 产品定位与技术底座

- 单机安卓 App，纯离线，无账号、无服务器、无联网依赖。
- Godot 4 + GDScript（standard 版，非 .NET）。
- **安卓工具链严格按所安装 Godot stable 版本的官方《Exporting for Android》文档配置。本文档不手写 SDK/NDK/JDK 版本号，防止过期腐烂。**
- 范围路线：v1 一个活的郡 → 最终全天下，且每一格地图都是活的。
- 一郡是切片，不是阉割版。扩张 = 加数据包 + 真实的系统扩张（见 §10，不承认“后期纯堆内容”）。
- 真三国，184 年颍川郡开局（黄巾 vs 汉廷 + 溃贼，长社之战为终局攻城剧本）。

## 2. 第一原则（不可妥协）

1. **命名实体与历史事实永远只有一份精确状态。** 精度分层只改变计算频率与视觉投影，不改变事实身份。
2. **两个正交维度，不许混用**：状态权威（永久事实 / 聚合状态 / 可重建投影）× 模拟精度（Hot / Warm / Cold）。**Cold ≠ 不精确存储，Cold = 低频计算。**
3. **模拟层与渲染完全分离。** 模拟层零渲染依赖。
4. **存档 = Canonical WorldState 的序列化。** 可重建投影不进权威存档。
5. **逻辑事务 ≠ 磁盘事务。** World mutation 只改内存并标 dirty；durability checkpoint 才写盘（见 §8）。

注：第 1、2 条约束的是长期方向；M0 施工范围见 §0。

## 3. 三层结构

### 3.1 Canonical WorldState（唯一真相，进存档）

```
CanonicalWorldState
├── world                    # 世界时间、全局 flag
├── player                   # 玩家：一切随身状态都是 canonical
│   ├── region_id
│   ├── location_id
│   ├── position             # 安卓随时切后台，恢复必须回到原地，不是城门口
│   ├── party
│   ├── inventory
│   ├── stats                # 武勇 / 智略 / 统率
│   ├── office               # 官职
│   └── active_events        # 任务/事件进度
├── factions                 # 势力
├── settlements              # 城池与归属
├── named_officers           # 命名武将
├── persistent_armies        # 持久军队（全局实体，见 §6）
├── relations                # 当前关系值（current state）
├── fact_journal             # 历史事实（为什么变成这样）
├── instantiated_events      # 已实例化事件
└── region_aggregate_state   # 区域聚合状态
```

**当前状态与历史事实的职责钉死**：`relations` 存当前值（`relation[guan_yu][player] = +35`）；`fact_journal` 存为什么（追加式记录：何时、何事、增减多少）。关系系统改当前值的同时追加事实；**禁止**从 journal 重建当前世界（那是偷偷做 event sourcing，不做）。Rumor 和剧情读 journal；当前世界只读 current state。

M0 只实现其中 player / world 所需的最小子集（§0）。

### 3.2 Active Region Projection（可重建，不进存档）

地图实体坐标、无名居民、巡逻与商队视觉实例、商店当日货物、临时遭遇、战场 Actors、粒子掉落动画。进入区域时依据永久事实 + 聚合状态 + 区域种子重新生成。

### 3.3 Region Pack（静态内容包）

```
regions/yingchuan/
├── map.json        # 地形、道路、城池村庄野外点位、探索点
├── cities.json     # 初始城池定义
├── officers.json   # 本郡登场武将引用表
├── items.json      # 有名装备定义
├── events/         # 本地事件定义（trigger / choices / outcomes）
├── dialogue/
└── assets/
```

- 城、武将、势力、事件、装备全部**全局唯一 ID**，region 只是归属字段。
- 事件带作用域字段 `scope: region | global`，v1 只用 region，字段从第一天存在。

> **已知长期改进（仅记录，无施工权）**：定义与出现应分离——`data/global/`（officers / factions / items 定义“它是谁”）+ `regions/xxx/`（officer_spawns / item_sources / events 定义“它为什么、在哪里出现”）。否则武将跨区流动、跨包引用唯一装备时会产生包间定义依赖。**现在不施工，D1 没有此需求。**

## 4. 状态权威分层（与模拟精度正交）

| 权威类型 | 内容 | 行为 |
|---|---|---|
| **永久精确状态** | 命名武将；城池归属；势力关系；有身份的军队与统帅；死亡/受伤/俘虏/投降/婚盟/仇恨；已触发关键事件；战争与围城；玩家影响过的事实 | 无论多远，永远精确 |
| **区域聚合状态** | 人口、粮食、钱粮、治安、繁荣、无名守军池、征兵能力、商业产出 | 远方按聚合值低频结算 |
| **可重建投影** | 无名村民、临时山贼、商店当日货、野外动物、视觉小兵、粒子尸体掉落、随机巡逻队坐标 | 不在场可丢弃，按种子重建 |

## 5. 系统拥有频率（实体不声明 LOD）

实体只存事实，不决定自己多久运行一次。频率归系统：LocalSimulationSystem（活跃郡，每 tick）、StrategicArmySystem（全部军队，分区域步长）、FactionDecisionSystem（势力决策，每若干 tick）、EconomySystem（聚合演化/物价/商路，按天）、HistoryEventSystem（机会窗口，按条件）、RumorSystem（journal → 传闻）。

## 6. 区域升降级

Cold/Warm 进入 Hot：宏观模拟追平世界时间 → 读永久事实 → 按聚合状态 + 种子生成投影 → 进入高精度模拟。离开时：永久变化折回 Canonical，丢弃投影。

**军队是全局实体**：`{army_id, region_id, route[waypoints], destination}`，区域场景只是投影，禁止两地各存一份。

## 7. 战斗

### 7.1 三段式边界

```
BattleSpec → RealTimeBattle 或 AutoResolver → BattleOutcome → ApplyBattleOutcome（幂等）
```

实时与公式结算共享输入/输出/世界后果，不共享执行代码。BattleOutcome 统一字段：胜负、双方伤亡、武将受伤/被俘/死亡、士气变化、战利品、城池归属变化、关系变化、`battle_id`。ApplyBattleOutcome 以 `battle_id` 幂等，只执行一次。

### 7.2 战斗崩溃恢复规则

进战斗写 BattleSpec + 战前 checkpoint → 战斗过程零持久化 → 进程被杀则回到战前。不恢复“挥刀第 17 帧”。Battle Actors 是真 Projection。

### 7.3 操作模型（定案，不再加料）

**主将直接控制 + 士兵自动作战。不是复杂 RTS，也不是纯自动。**

- 俯视 2D；**左摇杆移动主将**；普通攻击自动（索敌范围内自动出手）；
- 右侧最多两个按钮：**技能 + 军令**；
- 玩家真正管的只有三件事：**自己冲哪里、什么时候放技能、什么时候下军令**；
- 士兵自动就近索敌作战，不框选、不微操；
- 主将站位本身就是最大的战术输入。

**冻结条款**：不加翻滚、防御、三技能、兵种技能按钮。先验证“冲到敌后排一刀横扫”爽不爽。

### 7.4 战斗时长：调出来的分布，不是限时

不设硬倒计时。推导：核心冒险循环 5～10 分钟，战斗是其中一拍；两个主动操作 = 每 20～30 秒一个决策点，超过约 90 秒注意力必死（攻城 2～3 分钟须配额外决策点，M3 再做）。

机制保证：**大多数战斗的终点是一方士气崩溃（击溃），不是全歼。** 撤退永远可用；琐碎战斗可自动结算。

| 战斗类型 | 目标时长 |
|---|---|
| 碾压局 | 15～25 秒 |
| 均势局 | 40～70 秒 |
| 攻城（M3） | 2～3 分钟，配额外决策点 |

中位数超约 75 秒 → 上调杀伤 / 下调小兵血量，永远不加倒计时。验收看 20 场分布，不看单场。

## 8. 存档

逻辑事务 ≠ 磁盘事务：mutation 改内存标 dirty；checkpoint 原子写盘（临时文件 → 校验 → 替换正式存档）。

Checkpoint 触发：战斗结算、城池易主、招募/死亡/投降、关键事件完成 → **立即**；位置等高频变化 → **防抖**（几秒一次/进关键地点）；`on_pause` 仅补充；战斗前后各一次、战斗中不存（§7.2）。

只存 Canonical；`schema_version` + 迁移函数；三槽（自动 / 战斗前快照 / 手动）；种子入档 = 可复现可调试，不等于防 S/L。

M0 只实现“M0 实际需要的本地存档”（§0）：战前/战后 checkpoint + 防抖位置快照，其余无施工权。

## 9. 历史轨道（真三国长期项）

历史事件是有条件的机会窗口，不是按日期强制播放的时间表。条件（势力存在、敌对、控地、关键人物存活、年份入窗）不成立则取消、改名或生成替代历史事件。沙盒一致性优先于史实还原。

## 10. 里程碑（诚实版）

| 里程碑 | 内容 | 证明什么 |
|---|---|---|
| **M0** | 十分钟切片：功能灰盒 + 表现样片（§12） | 前十分钟让人愿意进入长期循环 |
| **M1** | 竖切：1 城 1 野 1 战 + 存档 | 冒险循环跑通 |
| **M2** | 活的一郡：world tick、势力 AI、事件系统、12 武将 | 世界会自己动 |
| **M3** | 攻城战、官职前四级、辅佐/自立双结局 | **v1.0 可发布** |
| **M4** | 跨区状态连续性专项（行军/补给/外交/围城/schema 升级等） | 世界比玩家大且不撕裂 |
| **M5** | 内容扩大生产，区域逐批点亮至全天下 | 全天下，每格都活 |

**v1 不做**：生活技能大杂烩、3D 化、任何联网功能、超过 12 个有名字的武将、活跃郡以外的随机事件、装备颜色数值阶梯。

## 11. 玩法支柱（驱动架构的设计决策）

> **内容清单效力说明**：本节出现的所有设施、装备、兵种、武将、事件清单，一律是**首批内容预算与示例，不是定死配置**。架构钉死的是规则（功能附着于人、装备机制差异、数据驱动），内容数量随时追加——加设施、加装备、加兵种 = 加数据条目，不动架构。

### 11.1 三层循环（核心循环定案）

| 层 | 时长 | 内容 |
|---|---|---|
| **即时循环** | 30 秒～2 分钟 | 看见东西 → 靠近 → 做决定 |
| **冒险循环** | 5～10 分钟 | 从一个地点出发 → 探索/战斗/任务 → 得到东西或改变关系 → 到达另一个地点 |
| **长期循环** | 20～40 分钟 | 做几件事 → 变强/招人/立功 → 身份变化 → 世界出现新的可能 |

M0 的意义：不是“十分钟跑完核心循环”，而是**验证前十分钟有没有足够的东西，让人愿意进入 20～40 分钟那一层**。

### 11.2 城市（可进入的小型真实场景）

**原则**：每座城市是一张紧凑、可自由移动的 2D 城市场景，由少量有空间关系的地点、NPC 和事件组成。**功能附着在具体人物与地点上，而不是固定菜单入口。**

**反模式（禁止）**：“酒馆=传闻 / 兵营=补兵 / 市场=买卖 / 官府=任务”四个系统按钮。进城 → 点四个按钮 → 出城，那不如 QQ 家园。

阳翟示例（2～4 屏的一小片城区）：

```
              郡府
               │
    豪族宅 ── 主街 ── 酒肆
       │        │
      小巷     市集 ── 铁匠铺
                │
              南城门
                │
               校场
```

玩家人物真的走。街上有人走动，商贩摆摊，兵从校场出来；某武将今天坐在酒肆，过几天已经不在了；受伤的黄巾逃兵可能躲在巷子里。建筑是世界里的地点，不是系统按钮。

**三城偏科（不是三套皮肤，不许每座城同一套设施）**：

下表设施为**首批内容**，后续可随时追加新建筑、新 NPC、新事件（加数据，不动架构）：

| 城 | 定位 | 设施（首批） | 这里发生的事 |
|---|---|---|---|
| 阳翟 | 郡治 | 郡府、官学/名士宅、较大市集、牢狱、校场 | 见官员、接官府差事、认识士人、升官、处理俘虏 |
| 长社 | 前线 | 城墙、军营、伤兵营、军需铺、破损民居（酒馆可能已被征作军用） | 征兵、军令、伤兵事件、逃兵、战时物价、守城准备 |
| 许县 | 商业/交通 | 驿站、大市、马行、客栈、商队驻地、铁匠 | 跑商、买坐骑、遇远方人物、听外地消息 |

玩家自然形成：**买马去许，入仕回阳翟，找军队去长社。** 这才叫地图有意义。

**功能挂在人身上**：进校场看见教官，和他讲话；白身时他说“军中可不是你这种游侠想来便来的”，有军职后再来才给兵——同一个地方随玩家身份改变意义。武将恰好在酒肆喝酒，明天可能跟商队走了，过几日甚至加入另一方——**找人本身就是游戏**。

**三个约束**：

1. **不求大，求有记忆点**：一座城 2～4 屏就够；
2. **不求设施齐全，求功能偏科**：每座城给玩家一个“为什么我要去那里”的理由；
3. **城市状态必须可见**：战争不是把 `war=true` 写进数据——城门多了守军、街上有伤兵、市集摊位减少、物价上涨、百姓议论、旗帜变化。玩家不打开状态面板都知道这城出事了。

保留：每城三值（繁荣/军备/民心）作为底层数据；城市仍是世界记忆的出口（传闻从酒肆 NPC 嘴里出来）；易主真实变化（旗帜、功能、物价）。

### 11.3 装备与寻宝（那个山洞里到底有什么）

- 地图必须有探索点：**山洞、废营、古墓、隐秘小路、破庙**，世界里可见，风险分档。
- 产出：特殊武器、坐骑、钱、宝物、**隐藏武将**、事件。
- **装备哲学：有名字、有机制区别的装备，胜过成批白绿蓝紫橙。** 每件带机制差异（古锭刀 = 攻击范围略大），带一句来历；不做颜色阶梯，不做强化膨胀。“30 件”只是 v1 的内容预算，**不是上限**——装备表是开放内容表，随时追加。
- **M0 一件就够，但第一件必须让玩家拿到后马上感受到行为变化**（见 §12.1）。
- 获取途径：探索点、武将随身、战功兑换、事件，全部可解释。

### 11.4 招募系统

- **士兵**：找校场的教官招募（功能附着于人，见 §11.2），受官职与城池军备影响；兵力上限随身份提升。
- **武将三个来源**（反抽卡，每个获得都可解释）：战场俘虏（打败 → 招降）、事件结识（凌燕式分支）、关系投靠（关系阈值 + 执念匹配）。武将在世界里有位置、会移动、会消失——找人本身就是游戏。
- **隐藏/特殊兵种**：不直接招募，通过事件、地点、武将解锁编制（例：某破庙事件解锁藤牌手；某武将自带先登营部曲）。兵种表数据化，带解锁条件字段。

## 12. M0：功能灰盒 + 表现样片（当前唯一施工区）

### 12.0 M0 双命题

“行为 → 回响”一个脚本任务也能做到，却仍然可能不好玩。M0 并列验证两个命题：

- **A. 空间诱惑**：玩家看见某件东西，会不会**主动偏离原计划**？
- **B. 世界回应**：玩家做过的事，会不会在别处产生**可感知的后果**？

要闭上的链：

```
看见东西 → 自己改变计划 → 做出选择 → 世界产生结果 → 结果制造下一个欲望
```

示例：本来去村庄 → 远处看见商队被堵 → 自己拐过去 → 打完看见废营地闪光 → 顺手去翻 → 跟商队回城 → 酒馆开始谈论自己刚才干的事。

这一刻出现的感觉是：“**我本来没准备干这个。**”——这比任务数量重要，是《三国大时代》那口气的核心。

### 12.1 地图：空间叙事小图

```
          山寨
           ▲
       林间小路
          ╱
阳翟城━━官道━━━━村庄
   ╲       ╲
    河滩     废营地
```

出城即见至少两个目的地；官道安全但可能遇山贼；树林能绕但可能遇伏；村民告知山寨位置；商队沿官道真实移动。

**废营地第一件宝物：长柄旧刀——横扫范围肉眼明显扩大。** 不要攻击 +7%。这件宝物直接教育玩家：地图角落值得我去翻。

### 12.2 遭遇：先在世界里被看见

不要黑屏弹窗。山贼远远堵路（可绕/可靠近/可拔刀/可跑）；商队真实移动，山贼抢商队是世界里正在发生的事；村民路口招手。

遭遇链互咬：村民告知山贼 → 山贼正在抢商队 → 救商队得钱 → 商人回阳翟 → 酒馆出现“有人击退官道贼寇”的传闻。

**M0 的阳翟按 §11.2 同一原则做最小形态**：一屏小街区（主街 + 酒肆），传闻从酒肆 NPC 嘴里说出来，不是弹出的菜单页。注：这使 D2–D3 的城市工作量从“热点页”变为“极小可行走场景”，范围微调但方向一致。

### 12.3 战斗规格

按 §7.3：左摇杆主将、普攻自动、一技能（横扫：伤害+击退）一军令（冲锋）、6v6+双方主将、士气崩溃即溃逃（逃跑+喊声+旗帜变化，必须明显）。时长按 §7.4 分布，实测 20 场统计。

### 12.4 表现要求（样片级，素材可临时但风格必须统一）

主将至少走/攻/受击动画；小兵基本动画；攻击有 hit-stop（60～90ms）、音效、尘土；溃逃有明确视听信号。不用灰盒胶囊测“好不好玩”。

### 12.5 数值假设表（全部可调，实测后替换）

| 项 | 假设值 |
|---|---|
| 主将移速 | 160 px/s |
| 遭遇可见距离 | 1.5 屏外可见 |
| 小兵 HP / 主将 HP | 20 / 60 |
| 小兵对砍倒地时间（1v1） | 约 3 秒 |
| 士气初始 / 崩溃阈值 | 100 / <30 |
| 主将阵亡士气冲击 | −30 |
| 队友倒地士气冲击 | −5/人 |
| 被围士气流失 | −8/s |
| 技能冷却 | 8 s |
| 战斗时长目标 | 碾压 15～25s；均势 40～70s |

### 12.6 测试协议与验收线（修订版）

- **前 10 分钟**：固定观察窗口，不引导、不提醒目标。
- **10 分钟到点**：不通知玩家、不结束游戏，再自由观察最多 5 分钟。（叫到停就永远观察不到“主动继续”。）
- **“主动第二轮”操作定义**：第一轮结果出现后，无弹窗要求、无强制任务箭头、测试者无提示；玩家在 2 分钟内自行选择一个新地点 / 新人物 / 新目标并开始行动。
  - “我想去看看那个山寨” = **PASS**；
  - 系统弹“下一任务：前往山寨”，照箭头走 = 不算；
  - “挺好玩的”然后停手 = 不算；
  - 开背包乱点两下 = 不算。
- **通过线**：≥4 人说得出“下一步想去干嘛”；≥3 人满足上面的操作定义。
- 不过线：改手感、密度、战斗节奏，**不回头改架构**。

### 12.7 D1：残酷范围（已批准）

Godot 工程 → 安卓横屏 → 这张极小地图 → 一个角色 → 相机 → 摇杆与点地 A/B → 真机 APK。**当天晚上真的拿手机在那片地上走。**

D1 只回答一个问题：**哪种移动让我愿意继续走？** 不舒服就改移动。

**D1 工程规格**（Godot 4 现行节点，不创建待迁移节点）：

```
luanshixing-m0/
├── project.godot          # 1280×720 视口；横屏；stretch=canvas_items, aspect=expand
├── scenes/
│   ├── Main.tscn
│   │   ├── Ground         # TileMapLayer（不用已 deprecated 的 TileMap）
│   │   ├── Decoration     # TileMapLayer
│   │   ├── Player         # CharacterBody2D + AnimatedSprite2D + Camera2D(smoothing 开)
│   │   └── UI             # 含 ABToggle
│   └── ui/ABToggle.tscn   # 调试按钮：一个 APK 内切换两种移动方案
├── scripts/
│   ├── player.gd          # 双输入方案 → 统一 velocity 接口
│   ├── joystick.gd        # 虚拟摇杆（触屏区域动态出现）
│   └── tap_to_move.gd     # 点地移动
└── assets/                # 临时但统一风格的角色/地块
```

**A/B 公平条款**：点地方案 D1 只做直线 + 碰撞滑动，无绕行导航。因此——A/B 只在没有复杂遮挡的开放区域对比测试；或之后给 tap-to-move 补最小导航，再做最终选择。**不允许让“点地不好用”的结论是寻路没实现造成的。**

**打包路径**：严格按所安装 Godot stable 版本的官方《Exporting for Android》文档配置（导出模板、SDK 配置、Android preset、导出 APK 或一键部署）。版本号以官方文档为准，本文档不写死。

**D1 验收**：真机上走 5 分钟，回答唯一问题——哪种移动让我愿意继续走？

### 12.8 D1–D7 节奏

D1 移动手感（上真机）→ D2–D3 地图 + 三种遭遇（世界内可见版）→ D4–D5 战斗 + 表现 → D6–D7 试玩（§12.6 协议）+ 调优，出结论。

### 12.9 纪律

- §0 是唯一施工权来源；本文其他一切均为 ACCEPTED HYPOTHESES；
- “顺便先创建接口方便以后扩展” = 打回；
- 移动手感第一天上真机，不在编辑器里自我感动。

---

## 变更记录

- **v0.2 → v0.3**：状态类型分层；系统拥有频率；军队全局实体；战斗三段式；历史机会窗口；写事务存档；里程碑诚实化；Fact Journal。
- **v0.3 → v0.3.1**：PlayerState 入 Canonical；Journal 职责钉死；逻辑/磁盘事务拆分；战斗崩溃恢复；权威 × 精度正交；战斗时长改调音分布；M0 升级“功能灰盒 + 表现样片”；遭遇世界内可见；D1 残酷范围。
- **v0.3.1 → v0.3.2**：工具链遵循官方文档；战斗操作模型定案；三层循环；玩法支柱（城市/装备寻宝/招募）。
- **v0.3.2 → v0.3.3**：新增 §0 当前施工权（Authority Split），状态改为“长期方向 ACCEPTED HYPOTHESES ｜ 施工 authority M0 only”；M0 改双命题（空间诱惑 × 世界回应）+ 欲望闭环；测试协议修订（10+5 分钟窗口、“主动第二轮”操作定义）；TileMap → TileMapLayer；A/B 公平条款；废营地首宝定为长柄旧刀（横扫范围肉眼扩大）；data/global 与 regions/ 拆分仅记录、无施工权；战斗操作模型加冻结条款。
- **v0.3.3 → v0.3.4**：城市推翻“四热点菜单”，改为可进入的小型真实场景（2～4 屏、有空间关系、功能附着于人与地点）；三城设施偏科（阳翟=郡治 / 长社=前线 / 许县=商埠），禁止三城同一套设施；新增城市三约束（求记忆点、求偏科、状态必须可见——war=true 不算数）；教官/武将随身份与时间在世界里改变意义，“找人本身就是游戏”；M0 阳翟同步为最小可行走街区（一屏 + 酒肆 NPC 承载传闻），D2–D3 范围微调。
- **v0.3.4 → v0.3.5**：新增内容清单效力说明——所有设施/装备/兵种/事件清单均为首批预算与示例，数据驱动、随时追加；“30 件装备”明确为 v1 预算非上限；三城设施表标注为首批内容。架构钉规则，不钉内容数量。
